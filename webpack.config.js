const path = require('path');

module.exports = {
  // Outras configurações...
  output: {
    path: path.resolve(__dirname, 'build'), // Saída para a pasta build
    filename: 'bundle.js', // Nome do arquivo de saída
  },
  // Outras configurações...
};
